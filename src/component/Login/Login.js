import React from 'react';
import NotFound from '../NotFound/NotFound';

const Login = () => {
    return (
        <div>
            <NotFound></NotFound>
        </div>
    );
};

export default Login;